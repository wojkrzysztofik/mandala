"use client";

import Image from "next/image";

export default function Footer() {
  return (
    <footer className="sticky top-[100vh] bg-gray-100 text-center text-sm p-10 mt-20">
      <p>mandala.store</p>
    </footer>
  );
}
